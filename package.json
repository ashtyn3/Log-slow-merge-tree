{
  "name": "db_idea",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
      "build": "bun run build.ts",
      "exec": "./db.out",
      "dev": "bun run build && bun run exec"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "sorted-btree": "^1.8.1"
  }
}
