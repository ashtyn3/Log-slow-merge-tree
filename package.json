{
  "name": "db_idea",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
      "build": "rm bin && bun run build.ts",
      "exec": "./bin",
      "dev": "bun run build && bun run exec"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "sorted-btree": "^1.8.1"
  }
}
